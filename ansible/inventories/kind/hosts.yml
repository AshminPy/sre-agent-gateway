---
# All tasks in this workflow run locally (gcloud/kubectl invoked from the control
# node against remote APIs/contexts) -- localhost is the only inventory host.
# Per-cluster targets are logical (external_clusters, in group_vars/all.yml), not
# separate Ansible inventory hosts.
all:
  hosts:
    localhost:
      ansible_connection: local
