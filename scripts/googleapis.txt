# Google API hosts the SRE agent calls, registered in Agent Registry so the
# Agent Gateway's default-deny egress allows them. Consumed by register_endpoints.py.
# Category headers below control which endpoint variants are generated.

# global and locational (by region & multi-region) endpoints
# {location}-{service}.googleapis.com  and  {service}.googleapis.com  (+ mtls)
aiplatform.googleapis.com

# global and locational (by region only) endpoints
agentregistry.googleapis.com

# global endpoints
# {service}.googleapis.com  (+ mtls)
container.googleapis.com
cloudresourcemanager.googleapis.com
logging.googleapis.com
monitoring.googleapis.com
oauth2.googleapis.com
# issue #130: was "trace.googleapis.com" -- confirmed live that host returns HTTP 404
# (not a real API), and Google's own Cloud Trace API v2 reference docs confirm the
# actual, documented service endpoint is cloudtrace.googleapis.com. The OTel
# CloudTraceSpanExporter's real client (google.cloud.trace_v2 TraceServiceClient)
# connects to cloudtrace.googleapis.com, confirmed via its own DEFAULT_ENDPOINT --
# the wrong hostname being registered here meant Cloud Trace egress was blocked by
# the gateway's default-deny policy exactly as if nothing had been registered at all.
cloudtrace.googleapis.com
telemetry.googleapis.com
iap.googleapis.com
storage.googleapis.com
iamcredentials.googleapis.com
sts.googleapis.com

# regional endpoints
# {service}.{region}.googleapis.com  (+ mtls)
modelarmor.googleapis.com
